{
  "name": "prime-haven-workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "artifacts/prime-haven"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=artifacts/prime-haven",
    "build": "npm run build --workspace=artifacts/prime-haven",
    "serve": "npm run serve --workspace=artifacts/prime-haven",
    "typecheck": "npm run typecheck --workspace=artifacts/prime-haven",
    "lint": "npm run typecheck --workspace=artifacts/prime-haven"
  }
}