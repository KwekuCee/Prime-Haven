{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"const fs=require('fs'); ['package-lock.json','yarn.lock'].forEach(f=>{try{fs.unlinkSync(f)}catch(e){}}); const ua=process.env.npm_config_user_agent||''; const isCI=process.env.CI==='true'; if(!isCI && !/^pnpm\\//.test(ua)){console.error('Use pnpm instead'); process.exit(1);}\"",
    "build": "bun --cwd ./artifacts/prime-haven run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
